{!
    parameters:
        hex: hex code
        name: name of the colors
!}

<div class="color">
    <div class="color-sample {?light}light{/light}" style="background-color: {hex};">{hex}</div>
    <div class="color-name">{name}</div>
</div>