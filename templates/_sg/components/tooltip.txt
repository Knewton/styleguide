// tooltips are opt-in and needs to be setup, you could just add the
// following line.
$('[data-toggle="tooltip"]').tooltip();

// custom tooltip setup
$('#custom-tooltip').tooltip({
    title: 'a <b>custom</b> tooltip!<br/>With a second line.',
    html: true,
    placement: 'top'
});
