<label class="radio{?inline} inline{/inline}">
    <input type="radio" name="{?name}{name}{:else}name{/name}" value="value"{?checked} checked{/checked}{?disabled} disabled{/disabled} />
    <span>{label|s}</span>
</label>